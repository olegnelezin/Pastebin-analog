spring:
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5438/pastebin
    username: postgres
    password: 123
  liquibase:
    change-log: classpath:db/changelog/master-changelog.yml